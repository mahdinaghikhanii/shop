import 'package:flutter/material.dart';

class DialogDoneViews extends StatelessWidget {
  const DialogDoneViews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
